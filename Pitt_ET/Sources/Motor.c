/*
 * Motor.c
 *
 *  Created on: Mar 3, 2017
 *      Author: Martina M
 */

#include "Motor.h"
/*
void speedL(int dir, int speed)
{
	if( dir > 0)
	{
		DIR_R_SetVal(DIR_R_DeviceData); // Richtung definieren 1
	}else
	{
		DIR_R_ClrVal(DIR_R_DeviceData); // Richtung definieren 0
	}
	PWM_R_SetDutyMS(speed);

}

void speedR(int dir, int speed)
{
	if( dir > 0)
	{
		DIR_L_SetVal(DIR_R_DeviceData); // Richtung definieren 1
	}else
	{
		DIR_L_ClrVal(DIR_R_DeviceData); // Richtung definieren 0
	}
	PWM_L_SetDutyMS(speed);


}
*/
